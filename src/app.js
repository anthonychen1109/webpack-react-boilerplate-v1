import React, { Component } from 'react';
import ReactDOM from 'react-dom';

import './styles/styles.scss';

const template = <p>this is webpack!!</p>
ReactDOM.render(template, document.getElementById('app'));
